// Persistence is optional. The Colyseus server must remain runnable on the
// local LAN without Firebase during the M0–M2 demo milestones.
export { getFirebaseApp, getFirestoreDb } from "./firebase.js";

# Optional: required only when the future backend writes persistent Firestore data.
# The M0–M2 local-LAN demo does not require Firebase.
FIREBASE_PROJECT_ID=your-firebase-project-id

# Reserved for the future Colyseus server. Cloud Run supplies this automatically.
PORT=2567
